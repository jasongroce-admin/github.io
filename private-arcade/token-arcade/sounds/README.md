# Token Arcade sounds

Audio effects for `slots.html` are generated procedurally with the Web Audio API.

This directory intentionally avoids checked-in binary assets to keep patch tooling
compatible with text-only diff workflows.
